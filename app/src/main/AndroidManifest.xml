<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/rateMe"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RateMe">
        <activity
            android:name="RateMe.LoginActivity.Welcome"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="RateMe.LoginActivity.Register"
            android:exported="false" />
        <activity
            android:name="RateMe.LoginActivity.Login"
            android:exported="false" />
        <activity
            android:name="RateMe.MainActivity.MainActivity"
            android:exported="true" />
        <activity
            android:name="RateMe.ScanActivity.RateProductActivity.Rate.RateProduct">
        </activity>
    </application>

</manifest>