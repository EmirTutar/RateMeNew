<?xml version="1.0" encoding="utf-8"?><!--suppress XmlUnusedNamespaceDeclaration -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!--suppress CheckTagEmptyBody -->
    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/rateMe"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RateMe">
        <activity
            android:name="RateMe.LoginActivity.Welcome"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="RateMe.LoginActivity.Register"
            android:exported="false" />
        <activity
            android:name="RateMe.LoginActivity.Login"
            android:exported="false" />
        <activity
            android:name="RateMe.MainActivity.MainActivity"
            android:exported="true" />
        <activity
            android:name="RateMe.ScanActivity.RateProductActivity.Rate.RateProduct"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name="RateMe.SettingsActivity.ProfileActivity" />
        <activity android:name="RateMe.SettingsActivity.PermissionsActivity" />
        <activity android:name="RateMe.SettingsActivity.AboutUsActivity" />
    </application>

</manifest>